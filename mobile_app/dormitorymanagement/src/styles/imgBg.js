import { StyleSheet } from 'react-native';

const styleImgBg = StyleSheet.create({
  imageBackground: {
    flex: 1,
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export { styleImgBg };
