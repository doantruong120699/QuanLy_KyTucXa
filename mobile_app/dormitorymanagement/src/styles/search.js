import { StyleSheet } from 'react-native';

const styleSearch = StyleSheet.create({
  viewSearch: {
    alignItems: 'center',
    flexDirection: 'row',
    width: '90%',
    marginBottom: 10,
  },
  inputTextSearch: {
    flex: 1,
    elevation: 7,
    borderRadius: 20,
    backgroundColor: 'white',
    padding: 10,
    alignItems: 'center',
  },
});

export { styleSearch };
